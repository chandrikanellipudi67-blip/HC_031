import React from 'react';
import { Tablet, Calendar, FileText, QrCode } from 'lucide-react';

export const PillIcon = (props: any) => <Tablet {...props} />;
export const CalendarIcon = (props: any) => <Calendar {...props} />;
export const PdfIcon = (props: any) => <FileText {...props} />;
export const QRIcon = (props: any) => <QrCode {...props} />;

export default PillIcon;
